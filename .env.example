# iLe — environment variables example
# Copy to .env (gitignored) for local development.

PUBLIC_SITE_URL=https://ile-hair-harajuku.com

# microCMS (optional — site falls back to static data when omitted)
MICROCMS_SERVICE_DOMAIN=
MICROCMS_API_KEY=
